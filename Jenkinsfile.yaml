pipeline:

 agent:
   #any:
node:
label: "master"
 tools:
   maven: MAVEN_HOME

 stages:

   - stage: TEST
     
     steps:
      
      
       - git 'https://github.com/gkerinel/gkerinelProject.git'
  
 post:
   always:
     - cleanWs()
